# The agent's own toolchain (plan W13, §5). Separate from the node's depends/ Rust (1.63):
# nothing here enters the ycashd build. Bump by PR like any dependency.
[toolchain]
channel = "1.91.0"
components = ["clippy", "rustfmt"]
